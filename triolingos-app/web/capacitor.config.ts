import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nknet.triolingos',
  appName: 'Triolingos',
  webDir: 'out',
  android: { allowMixedContent: false },
};
export default config;
